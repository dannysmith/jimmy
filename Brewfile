# frozen_string_literal: true

# This Brewfile is deliberatley lightweight. It contains only
#   the stuff needed to work with this project, and assumes you
#   already have rbenv, ruby etc installed already.

# Project Dependencies
tap 'postgresql'
tap 'redis'
tap 'nodejs'
tap 'chromedriver'

# Development Tools
tap 'heroku'
tap 'overmind'
tap 'imagemagick'
